# Search
This directory contains the front-end components relating to Search.

`SearchViewer` handles displaying a search problem that can show the execution of the algorithm, highlighting the frontier and neighbouring nodes.

`SearchBuilder` provides a graphical interface to construct search problems that you can later solve visually with `SearchViewer`.

Check out [`/notebooks/search/`](/notebooks/search/) for examples.
